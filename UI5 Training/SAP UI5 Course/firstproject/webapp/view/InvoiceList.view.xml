<mvc:View
    controllerName="firstproject.controller.InvoiceList"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">

    <List id="invoiceList"
    headerText="{i18n>invoiceListTitle}"
    class="sapUiResponsiveMargin"
    width="auto"
    items="{
        path:'invoice>/Invoices',
        sorter:{
            path:'ProductName'
        }
        }">
     <headerToolbar>
            <Toolbar id="Toolbar1">
            <Title id="titleToolbar" text="{i18n>invoiceListTitle}"/>
            <ToolbarSpacer id="toolbarSpacer1"/>
            <SearchField id="searchField1" width="50%" search=".onFilterInvoices"/>
            </Toolbar>
        </headerToolbar>
    <items>
    <ObjectListItem  title="{invoice>Quantity} x {invoice>ProductName}" id="objectList1"
        number="{ 
            parts:[{path:'invoice>ExtendedPrice'},{path:'view>/currency'}],
            type:'sap.ui.model.type.Currency',
            formatOptions:{
                showMeasure:false
            }
         }"
         numberUnit="{view>/currency}"
         numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success' }"
    >
    <firstStatus>
        <ObjectStatus
        id='objectStatus1'
            text="{
                path:'invoice>Status',
                formatter:'.formatter.statusText'
            }"
         >
        </ObjectStatus>
    </firstStatus>
    </ObjectListItem>
    </items>
    </List>
</mvc:View>
